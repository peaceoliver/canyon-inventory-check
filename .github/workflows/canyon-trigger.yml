name: Canyon Trigger

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Call Streamlit App
        run: |
          curl -L "https://canyon.streamlit.app/?run=true"